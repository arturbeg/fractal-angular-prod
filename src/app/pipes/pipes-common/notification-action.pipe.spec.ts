import { NotificationActionPipe } from './notification-action.pipe';

describe('NotificationActionPipe', () => {
  it('create an instance', () => {
    const pipe = new NotificationActionPipe();
    expect(pipe).toBeTruthy();
  });
});
