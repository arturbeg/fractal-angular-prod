import { NotificationRedirectUrlPipe } from './notification-redirect-url.pipe';

describe('NotificationRedirectUrlPipe', () => {
  it('create an instance', () => {
    const pipe = new NotificationRedirectUrlPipe();
    expect(pipe).toBeTruthy();
  });
});
